[model]
state_dim = 14 
gamma = 0.8 


[init]
traj_dir = data/multi_sim
num_epochs = 250


[train]
batch_size = 500 
learning_rate = 0.025
step_size = 50 
train_episodes = 200
sample_episodes = 10
test_interval = 50 
test_episodes = 100 
capacity = 400000
epsilon_start = 0.5
epsilon_end = 0.1
epsilon_decay = 150 
num_epochs = 30 
checkpoint_interval = 30


[changed learning rate from 0.01 to 0.1, step size from 150 to 50]
[state_dim - own 9 states and 5 observed for the other agent]
[batch_size - [torch arg parameter, number of samples to load per batch]]
[capacity - number of past experiences (positions) that are kept - see ReplayMemory class in utils.py]
[epsilon = epsilon_start + (epsilon_end - epsilon_start) / epsilon_decay * episode]
[checkpoint - save model every 30 episodes]
